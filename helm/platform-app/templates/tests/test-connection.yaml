apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platform-app.fullname" . }}-test-connection"
  labels:
    {{- include "platform-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platform-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
